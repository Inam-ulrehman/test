import { ConfigProvider, theme } from 'antd'

import React from 'react'

const customTheme = () => {
  let theme = {
    token: {
      colorPrimary: '#00b96b',
    },
  }
  return theme
}

export default customTheme
